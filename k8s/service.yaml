apiVersion: v1
kind: Service
metadata:
  name: audit-log-service-node
  labels:
    app: audit-log-service-node
spec:
  type: NodePort
  selector:
    app: audit-log-service-node
  ports:
    - protocol: TCP
      port: 3012
      targetPort: 3012